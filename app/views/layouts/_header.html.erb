<div class="py-3 text-white top-header">
  <% if user_signed_in? %>
    <%= link_to(user_lists_path(current_user)) do %>
      <h3 class="text-center font-weight-bold text-white">アルカナ</h3>
    <% end %>
  <% else %>
     <h3 class="text-center font-weight-bold text-white">アルカナ</h3>
  <% end %>
   <small><%= link_to 'ログアウト', destroy_user_session_path, method: :delete %></small>
</div>